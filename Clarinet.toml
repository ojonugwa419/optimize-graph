[project]
name = "optimize-graph"
authors = ["Blockchain Goal Tracking Team"]
description = "A decentralized goal optimization and tracking platform"
telemetry = false
requirements = []
[contracts.goal-optimizer]
path = "contracts/goal-optimizer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
